/**
 * Shared Types Index
 *
 * Central export point for all shared TypeScript types used across
 * main process, preload scripts, and renderer process.
 */

// Database entity types
export * from './database';

// IPC communication types
export * from './ipc';
